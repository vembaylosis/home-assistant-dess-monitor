"""Legacy ``api`` package — transport moved to :mod:`custom_components.dess_monitor.sdk`.

Only :mod:`api.helpers`, :mod:`api.commands`, and :mod:`api.resolvers` remain here as
domain logic that complements the SDK. Direct imports from this package's top level
have been removed; import from the relevant submodule instead.
"""
